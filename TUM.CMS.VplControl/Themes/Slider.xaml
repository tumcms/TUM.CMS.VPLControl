<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:nodes="clr-namespace:TUM.CMS.VplControl.Nodes">


    <Style x:Key="ExpanderSliderStyleDouble" TargetType="nodes:SliderExpanderDouble">
        <Setter Property="Width" Value="250" />
        
        <Setter Property="HeaderTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Grid Height="40" Width="220">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>

                        <Slider Name="Slider"
                                Minimum="{Binding SliderMin, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type nodes:SliderExpanderDouble}}}"
                                Maximum="{Binding SliderMax, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type nodes:SliderExpanderDouble}}}"
                                Value="{Binding SliderValue, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type nodes:SliderExpanderDouble}}}"
                                TickFrequency="{Binding SliderStep, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type nodes:SliderExpanderDouble}}}" 
                                IsSnapToTickEnabled="True"
                                />
                        
                        <TextBox Grid.Column="1"
                                 Text="{Binding SliderValue, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type nodes:SliderExpanderDouble}}}" />
                    </Grid>
                </DataTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="2*" />
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="40" />
                            <RowDefinition Height="40" />
                            <RowDefinition Height="40" />
                        </Grid.RowDefinitions>

                        <Label Grid.Row="0" Grid.Column="0">Min</Label>
                        <Label Grid.Row="1" Grid.Column="0">Max</Label>
                        <Label Grid.Row="2" Grid.Column="0">Step</Label>

                        <TextBox Grid.Row="0" Grid.Column="1" Name="TextBoxMin" Text="{Binding SliderMin, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type nodes:SliderExpanderDouble}}}" />
                        <TextBox Grid.Row="1" Grid.Column="1" Name="TextBoxMax" Text="{Binding SliderMax, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type nodes:SliderExpanderDouble}}}" />
                        <TextBox Grid.Row="2" Grid.Column="1" Name="TextBoxStep" Text="{Binding SliderStep, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type nodes:SliderExpanderDouble}}}"/>

                    </Grid>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style x:Key="ExpanderSliderStyleInteger" TargetType="nodes:SliderExpanderInteger">
        <Setter Property="Width" Value="250" />

        <Setter Property="HeaderTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Grid Height="40" Width="220">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>

                        <Slider Name="Slider"
                                Minimum="{Binding SliderMin, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type nodes:SliderExpanderInteger}}}"
                                Maximum="{Binding SliderMax, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type nodes:SliderExpanderInteger}}}"
                                Value="{Binding SliderValue, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type nodes:SliderExpanderInteger}}}"
                                TickFrequency="{Binding SliderStep, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type nodes:SliderExpanderInteger}}}" 
                                IsSnapToTickEnabled="True"
                                />

                        <TextBox Grid.Column="1"
                                 Text="{Binding SliderValue, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type nodes:SliderExpanderInteger}}}" />
                    </Grid>
                </DataTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="2*" />
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="40" />
                            <RowDefinition Height="40" />
                            <RowDefinition Height="40" />
                        </Grid.RowDefinitions>

                        <Label Grid.Row="0" Grid.Column="0">Min</Label>
                        <Label Grid.Row="1" Grid.Column="0">Max</Label>
                        <Label Grid.Row="2" Grid.Column="0">Step</Label>

                        <TextBox Grid.Row="0" Grid.Column="1" Name="TextBoxMin" Text="{Binding SliderMin, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type nodes:SliderExpanderInteger}}}" />
                        <TextBox Grid.Row="1" Grid.Column="1" Name="TextBoxMax" Text="{Binding SliderMax, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type nodes:SliderExpanderInteger}}}" />
                        <TextBox Grid.Row="2" Grid.Column="1" Name="TextBoxStep" Text="{Binding SliderStep, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type nodes:SliderExpanderInteger}}}"/>

                    </Grid>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>


</ResourceDictionary>